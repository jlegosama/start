var util=require("../"),should=require("should"),path=require("path"),through=require("through2");require("mocha");describe("isStream()",function(){it("should work on a stream",function(e){util.isStream(through()).should.equal(!0);e()});it("should not work on a buffer",function(e){util.isStream(new Buffer("huh")).should.equal(!1);e()})});