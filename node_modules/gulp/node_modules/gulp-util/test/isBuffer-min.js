var util=require("../"),should=require("should"),path=require("path"),through=require("through2");require("mocha");describe("isBuffer()",function(){it("should work on a buffer",function(e){util.isBuffer(new Buffer("huh")).should.equal(!0);e()});it("should not work on a stream",function(e){util.isBuffer(through()).should.equal(!1);e()})});