function write(){written++}function ondata(){data++}var through=require("through"),test=require("tape"),duplex=require("../index"),readable=through(),writable=through(write),written=0,data=0,stream=duplex(writable,readable);stream.on("data",ondata);test("emit and write",function(e){e.plan(2);stream.write();readable.emit("data");e.equal(written,1,"should have written once");e.equal(data,1,"should have recived once")});